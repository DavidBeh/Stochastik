using System.Collections;

namespace Stochastik;

public static class HelperExtensions
{
    /*
    public static T Ausgeben<T>(this T liste, string? notiz = null) where T : IEnumerable
    {
        Console.WriteLine((notiz is not null or "" ? notiz + " " : "") + string.Join(", ", liste.Cast<object>()));
        return liste;
    }*/
}